# Frontend

本地 React 前端骨架。

## 职责

1. 对话生图和参数审核。
2. 工作流模板导入和简化表单运行。
3. 内容仓库、角色仓库、LoRA 管理。
4. 后续轻量画布。

## 计划启动命令

```powershell
cd frontend
npm install
npm run dev
```
## AI 配置页

用于配置基础 AI 生图所需的 URL、API Key 和模型名。模型列表可自动获取，失败时支持手动输入。